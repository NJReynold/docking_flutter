import 'package:docking/docking.dart' show DockingArea;
import 'package:docking/src/layout/docking_layout.dart' show DockingArea;

/// Represents the [DockingArea] type.
enum DockingAreaType { item, tabs, row, column }
